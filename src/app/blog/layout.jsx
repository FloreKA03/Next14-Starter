const BlogLayout = ({children}) => {
    return (
        <div>
            <h1>Blog Layout</h1>
            <p>This is the Blog Layout.</p>
            {children}
        </div>
    );
};

export default BlogLayout;